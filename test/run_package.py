"""
follow the steps:
    pip install pyapp-window
    python -m pyapp_window -h
check if the output is:
                                               python -m pyapp_window ...
╭───────────────────────────────────────────────────────────────────────────────────────────────────────── PARAMETERS ─╮
│    1    url              TEXT                                                                default = :none         │
│    2    port       -p    NUMBER    if `url` is not specified but `port` is set, it will      default = :none         │
│                                    open a localhost url.                                                             │
│    3    host             TEXT                                                                default = :none         │
│    4    size       -s    TEXT                                                                default = 1600x1200     │
│    5    title            TEXT                                                                default = PyApp Window  │
│    6    pos              TEXT                                                                default = center        │
│    7    backend          TEXT                                                                default = :none         │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
"""
